let initialState = {
    itemNames: [
        {
            itemName: '',
            itemName: '',
            itemName: '',
        }
    ],
    titles: [
        {
            title: 'Меню',
        }
    ]
}

const MenuReducer = (state = initialState, action) => {
    return state
}

export default MenuReducer