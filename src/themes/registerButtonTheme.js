import { createTheme } from "@mui/material";


const theme = createTheme({
    palette: {
      primary: {
        main: '#ffff'
      },
      secondary: {
        main: '#f5f5f5'
      }
    }
   })

export default createTheme(theme);